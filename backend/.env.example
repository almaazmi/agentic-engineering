# Backend runtime configuration (copy to .env for local dev)
APP_ENVIRONMENT=local
APP_APP_NAME=agentic-engineering-backend
# Comma-separated allowed CORS origins (use the frontend URL in prod)
APP_CORS_ORIGINS=http://localhost:3000
